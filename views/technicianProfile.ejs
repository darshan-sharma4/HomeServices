<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Technician Profiles</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fadeInUp {
      animation: fadeInUp 0.6s ease-out;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-blue-50 via-white to-blue-100 text-gray-800 min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-white shadow-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-3xl font-extrabold text-blue-700 tracking-wide">Technician Profiles</h1>
      <a href="/" class="text-blue-600 font-medium hover:underline text-lg">← Back to Home</a>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-1 max-w-6xl mx-auto px-4 py-12">
    <h2 class="text-4xl font-bold text-center text-blue-700 mb-14 drop-shadow-md animate-fadeInUp">Available <%= service
        %>s</h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-10">
      <% technician.forEach(tech=> { %>
        <div
          class="bg-white rounded-2xl p-6 shadow-lg border border-blue-100 hover:shadow-2xl hover:border-blue-400 transform hover:scale-105 transition-all duration-300 animate-fadeInUp">
          <div class="flex items-center mb-4 space-x-4">
            <img src="https://api.dicebear.com/7.x/initials/svg?seed=<%= tech.name %>" alt="Avatar"
              class="w-14 h-14 rounded-full border-2 border-blue-400 shadow" />
            <div>
              <h3 class="text-xl font-bold text-blue-800 leading-snug">
                <%= tech.name %>
              </h3>
              <p class="text-xs text-gray-500 capitalize tracking-wide">
                <%= tech.service %>
              </p>
            </div>
          </div>

          <div class="space-y-2 text-sm text-gray-700 mb-4">
            <p><strong>📍 Location:</strong>
              <%= tech.location %>
            </p>
            <p><strong>💰 Starting Charge:</strong> ₹99</p>

            <% if (user) { %>
              <p><strong>📞 Contact:</strong>
                <%= tech.phone %>
              </p>
              <% } %>
          </div>

          <% if (user) { %>
            <a href="/hire/<%= tech.technicianId %>"
              class="inline-block w-full text-center bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 shadow transition">Hire
              Now</a>
            <% } else { %>
              <a href="/user/login"
                class="inline-block w-full text-center bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 shadow transition">Login
                to Hire</a>
              <% } %>

        </div>
        <% }) %>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white mt-12 shadow-inner">
    <div class="text-center py-6 text-sm">&copy; 2025 HomeServices. All rights reserved.</div>
  </footer>

</body>

</html>